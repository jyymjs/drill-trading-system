# 错误台账（运行错误登记 · 2026-08-06 老板拍板建立）

> 规则：运行过程中出现的**任何错误**（脚本报错/数据问题/实验失败/流程违规）→ 登记本台账。
> 每条必须：现象 → 原因分析 → 解决方案 → 状态。**重要性等同待办**：未解决项主对话自动排期处理，老板可随时过目。
> 处理流程：登记 → 找原因 → 出方案 → 修复/豁免 → 状态更新。主对话遇到新错误先查本台账（防重复排查）。
> **处理权与报告义务（2026-08-06 老板授权）**：台账事项由**助理自主决定处理方式**（修/缓/豁免），无需逐件请示，**处理照常及时进行**；**处理结果统一放进周报呈报**（不单独递报告）；决定不处理的必须说明原因；处理不了或需老板决策的上报并在周报中注明。

| 编号 | 日期 | 错误现象 | 原因分析 | 解决方案 | 状态 |
|---|---|---|---|---|---|
| E-001 | 08-06 | 对比实验脚本 `--smoke` 模式覆盖正式报告（**踩 2 次**：T-024 工程部+质检各踩一次，T-026 冒烟又覆盖 T-024 报告） | smoke 不传 --out 时写默认输出路径，与正式报告同路径 | 所有实验脚本 smoke 输出带 `_smoke` 后缀或拒绝覆盖已有报告；质检已建议，待统一修复 | 🔧 待修复（高优先） |
| E-002 | 08-06 | 工程 worktree 初始化停留在旧 HEAD（T-016/工程I 等多次遇到），导致基于旧结构施工 | worktree 创建时未 reset 到最新 main | 工程部开工前统一 `git reset --hard main` 检查；派工说明已含，待固化到 executor.md | 🔧 待固化 |
| E-003 | 08-06 | sim_capital `--help` 崩溃（argparse 裸 `%`） | help 字符串 `1.5%` 中 % 后跟中文逗号触发 format 异常 | 已修：`1.5%%`（随 T-023 合并） | ✅ 已修复 |
| E-004 | 08-06 | 每日扫描.ps1 合并后丢失 UTF-8 BOM（GBK 控制台中文路径乱码风险） | 工程部生成时未保留 BOM，与工作区旧版差异 | 已修：恢复 BOM+CRLF（b0a7c21）；PowerShell 解析验证通过 | ✅ 已修复 |
| E-005 | 08-06 | 每日扫描.ps1 报告复制源路径错位（复制 07-30 旧文件） | 原脚本从 `产出\输出` 找 scan_result，实际输出在 `数据基础\output` | 已修：源路径修正（随扫描增强合并） | ✅ 已修复 |
| E-006 | 08-06 | sim_capital 买入价口径 bug（18/79 笔 R 与盈亏符号矛盾） | 旧版用信号日收盘价成交，与 R 的触发价口径不一致 | 已修：entry_20d 触发价口径（质检 28d8e5d 合并） | ✅ 已修复 |
| E-007 | 08-06 | 网格实验未成交 avgR 差集口径错误（稀释 30 倍） | 差集前未按 simulate_capital 同口径过滤（未触发信号 r=0 混入） | 已修：先过滤再差集（质检复验通过，随 T-023 合并） | ✅ 已修复 |
| E-008 | 08-06 | 量比 1.5~2.0 甜点结论（抽样 62 笔）与全量相反 | 小样本偶然（方法论：抽样实验必须全量复核） | 已修：T-025 2×2 矩阵定位，X=1.5 降为下限，表述改"量比越高越好" | ✅ 已修复（铁律已入记忆） |
| E-009 | 08-06 | GBK 控制台下 reporter ¥ 符号 UnicodeEncodeError（存量） | 终端编码与源码 UTF-8 不一致（GBK 默认） | 编码统一处理（候选小项） | ⏸ 待处理（低优先） |
| E-010 | 08-06 | calc_trade_fee 买入侧误扣印花税（存量保守口径） | 函数实现无条件加印花税，注释说"仅卖出" | 列入风控模块待修清单（12 组统一可比，不阻塞） | ⏸ 待处理（低优先） |
| E-011 | 08-06 | T-016 benchmark 脚本 6 vs 12 workers 结果不一致 | benchmark 缺 `__main__` 保护 → Windows spawn 递归污染（引擎本身确定） | 已识别，benchmark 已修；引擎并发确定性经验证 | ✅ 已解决 |
| E-012 | 08-06 | T-025 默认模式 KeyError 崩溃 | `_verdict` 硬编码四格访问，默认只跑抽样两格 | 已修：cells.get 容错 + docstring 同步（复核通过合并） | ✅ 已修复 |
| E-013 | 08-06 | sim_capital `--c23` help 崩溃（argparse 裸 `%` 再次出现，同 E-003） | help 字符串含未转义 %（这次是 C23 描述） | 已修：转义（工程K 顺手修复）；**教训：新增 argparse help 一律检查 %** | ✅ 已修复 |
| E-014 | 08-06 | 测试文档 `测试方案-T4-评级一致性.md` 仍引用已删除的 `数据基础/数据/cache`（质检多进程 P2） | CSV 死数据清理后文档未同步 | 更新文档措辞（引用现 fallback 链） | ⏸ 待处理（低优先） |
| E-015 | 08-06 | report.py 打印"并发：N 线程"，实际为进程（质检多进程 P3） | 沿用既有文案 | 改"进程" | ⏸ 待处理（低优先） |
| E-016 | 08-06 | 指数缓存覆盖不足时（<120 根）全部信号静默归"震荡"，报告无提示（质检市场分段） | MA120 全 NaN 时规则兜底归震荡 | 报告加"指数覆盖不足"说明 | ⏸ 待处理（低优先） |
| E-017 | 08-06 | 对比实验脚本 recompute 复算逻辑无单测（质检收紧测试建议） | 依赖重跑验证 | 补轻量单测 | ⏸ 待处理（低优先，可选） |
| E-018 | 08-06 | sim_capital `--c23` 统计打印留存 107.6% 荒谬（质检 C23 接入①） | mask 应用于全表未先过滤触发集 | 先构造触发集再 mask 再统计（已派工程L） | 🔧 修复中 |
| E-019 | 08-06 | 策略版本存档复现命令缺 `--signals`，复现不出存档数字（质检 C23 接入②） | 默认信号源是旧版无 dn_confirm 过滤 | 命令补 --signals（已派工程L） | 🔧 修复中 |
| E-020 | 08-06 | test_scan C23 动量拒绝用例弱断言（质检 C23 接入④） | 条件分支断言未锁前提 | 改确定断言（已派工程L） | 🔧 修复中 |
| E-021 | 08-06 | 常量两处同步风险（scanner 自持 vs tighten_compare）（质检 C23 接入⑤） | 包路径限制，注释已明示 | 长期共享常量模块，暂缓（注释+存档已记录） | ⏸ 缓办（周报说明） |
| E-022 | 08-06 | 历史实验报告 `突破日量能确认实验` 旧文案残留 1 处（质检 C23 接入⑥） | T-025 修正前的历史报告，非指引本体 | 豁免：历史报告不改，指引文档已全部修正 | ⏸ 豁免（周报说明） |
| E-023 | 08-06 | C23 版网格报告回撤仍用现金口径（绝对数字夸大 3-4 倍，老板拷问"为什么还在用现金口径"） | 主对话派工时定"沿用现金口径保组间可比"，牺牲绝对真实性 | ✅ 已修：12 组总资产口径重算（c77b92f）+ 报告双口径并存；口径铁律已入台账 | ✅ 已修复 |
| E-030 | 08-06 | capital_dd_recalc_grid.py import 未排序（质检 真实回撤①） | ruff I001 | ruff check --fix 一行 | ⏸ 待处理（低优先） |
| E-031 | 08-06 | "12 组补真实回撤"无独立确认书登记（质检 真实回撤②） | T 系列惯例（commit+报告延伸说明） | 待办档案/注册表补登记（本台账已登记，随周报） | ⏸ 待处理（低优先） |
| E-032 | 08-06 | `_compare_lines` 硬编码解包 3 行与配置列表强耦合（质检 三档蒙特卡洛①） | 未来扩展配置会静默错位 | 断言 len(rows)==3 或按配置循环渲染 | ⏸ 待处理（低优先） |
| E-033 | 08-06 | --compare 与 --risk-pct/--max-positions 同时传参后者被静默忽略（质检 三档蒙特卡洛②） | argparse 无互斥 | 互斥组或文档注明 | ⏸ 待处理（低优先） |
| E-041 | 08-06 | G7 报告诊断数字硬编码不可复算（质检 中批次B①） | render_markdown 硬编码 adv 统计 | 诊断逻辑入脚本+标注口径 | ⏸ 待处理（低优先） |
| E-042 | 08-06 | test_g4_sharp_v 注释与实现不符（质检 中批次B②） | 注释写实体 0/影线 0.7 实际 0.3/0.4 | 修注释+补显式锁定真实开盘价用例 | ⏸ 待处理（低优先） |
| E-043 | 08-06 | 提交信息"实体恒=0"表述不准（实为实体=\|收盘价\|）（质检 中批次B③） | 措辞 | 修正注释 | ⏸ 待处理（低优先） |
| E-044 | 08-06 | TTP 边界口径 >=5R vs 原文"大于5R"（质检 中批次B④） | 全系统统一含 5R 边界 | 注释标注工程定案 | ⏸ 待处理（低优先） |
| E-024 | 08-06 | `_verdict_c23` 网格<12 组（冒烟）时输出"——0/4 档"异常文本（质检 C23网格①） | 缺空组保护 | len(rows)<12 时跳过对照或加空保护 | ⏸ 待处理（低优先） |
| E-025 | 08-06 | c23_mask 仍有 2 处历史副本（c23_capital_compare/regime_segment_compare）（质检 C23网格②） | 历史脚本早于单一来源收敛 | 后续收敛至 sim_capital 单一来源 | ⏸ 待处理（低优先） |
| E-026 | 08-06 | V1_BEST 对照组硬编码在脚本（质检 C23网格③） | 注释已标明出处 | T-023 报告重跑时同步更新 | ⏸ 待处理（低优先） |
| E-027 | 08-06 | 蒙特卡洛报告 avgR 未对照既有引擎口径 0.683（含费 0.646 差异来源未说明）（质检蒙特卡洛①） | 报告已标"含费"但未跨报告对照 | 报告补一句口径对照说明（差值=每笔费用约 0.037R） | ⏸ 待处理（低优先） |
| E-028 | 08-06 | 报告渲染 f-string 与 + 拼接混用（质检蒙特卡洛②） | 风格小瑕疵 | 顺手统一 | ⏸ 待处理（低优先） |
| E-029 | 08-06 | simulate 内部 r!=0 过滤静默剔除零盈亏成交（质检蒙特卡洛③） | 实测 0 笔无影响 | capital_trade_r 注释说明 | ⏸ 待处理（低优先） |

> **机制固化（2026-08-06 老板拷问后）**：质检报告建议级问题一律登记本台账（来源=质检 XX），与运行错误同等跟进；低优先项在日报"逾期未办"窗口内滚动，不随会话遗忘。
> **口径铁律（2026-08-06 老板拷问后）**：给老板看的回撤/收益绝对数字一律用**总资产口径**（现金+持仓市值）；现金口径仅作组间对比列并存标注，**绝不只给现金口径**。
> 维护：主对话负责登记与跟进；新错误优先查本表 → 无记录则登记并排查。
