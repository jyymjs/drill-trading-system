# 架构规范

## 通用分层原则

- 分层依赖单向：上层只依赖下层，禁止反向依赖与跨层依赖
- 模块边界清晰：模块间通过接口/函数签名通信，不直接渗透对方内部实现
- 单一职责：每个模块只为一个原因变化
- 新增文件遵从既有目录结构

## 示例（量化交易项目分层）

config → data → analysis → strategy → risk → 输出

> 各项目在自身 CLAUDE.md 中定义具体分层，本规则只约束分层的通用性质。
