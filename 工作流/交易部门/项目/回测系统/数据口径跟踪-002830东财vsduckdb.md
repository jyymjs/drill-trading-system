# 数据口径差异跟踪：002830（东财 vs duckdb）— 2026-08-06 立项

> 来源：T-017 P2 全量对账报告（4.2 节）+ P5 变更说明（误差对照节）。
> 本文件为**跟踪载体**，仅记录现象与待办，**不修数据**（工程任务边界：T-016 收尾）。

## 一、问题现象

| 维度 | 数值 |
|---|---|
| 案例股票 | 002830（名臣健康） |
| duckdb vs akshare(东财) 全历史中位误差 | **0.48%**（P2 全量抽样，落在"需关注 0.1%~0.5%"档） |
| duckdb vs 东财+新浪 近 90 日窗口误差 | **0.43%**（P5 实测） |
| 双侧交叉验证 | 东财与新浪**双源一致** → 差异不在东财侧，而在 **duckdb 侧的通达信除权因子基准** |

定位：duckdb 主库（`数据基础/data/t017_p2.duckdb`）以通达信 pytdx 为原始源、
`compute_qfq`（等比因子法）自算前复权；与东财/新浪的复权基准存在系统性小差
（小比例分红除权日口径差异，10 派 1 元以内量级）。

## 二、影响范围

- **对账口径内**：0.48% < recon 0.5% 报警线（P2 设置 `RECON_ALERT_PCT = 0.5`），
  未触发报警，符合"通过"判定；
- **同类疑难**（P1/P2 遗留，均非 T-017 引入）：
  - 000651 格力 0.95%——1990s 送转记录口径差，**2015 年后一致**；维持 RECON_MUST 跟踪；
  - 300093 金刚光伏 13.5%——通达信 xdxr **漏记除权**（已由 `XDXR_JUMP_THRESHOLD=0.05`
    启发式覆盖，见 `数据基础/duckdb/config.py`），与 002830 性质不同（漏记 vs 基准差）；
- **策略影响评估**（P2 全量报告 4.2 节结论）：002830 类小比例分红除权口径差
  **不影响涨跌幅类策略**（回测主用）；仅对复权价格绝对值敏感的统计口径
  （如价格锚点类指标）存在 0.5% 内偏差，可接受。

## 三、待处理事项

- [ ] 单独立项跟踪（P2 遗留，非 P5 引入；本项目仅建立跟踪文档）
- [ ] 老板决策：002830 是否纳入 `RECON_MUST` 长期对账名单（当前名单 000651/600519）
- [ ] 若未来上线对**复权绝对值**敏感的策略（价格锚点/分红率类），先核对该股
      通达信 xdxr 记录与东财除权记录差异明细再放行
- [ ] 定期复查：每次全量对账（P2 抽样 50 只）观察该股误差是否随除权记录补全收敛

## 四、关联文档

- `数据基础/duckdb/P5-变更说明.md`（误差对照节、遗留事项第 2 条）
- `产出/输出/t017_p2_全量报告.md`（4.2 节误差分档表）
- `数据基础/duckdb/recon.py`（对账实现，RECON_MUST/RECON_ALERT_PCT）
