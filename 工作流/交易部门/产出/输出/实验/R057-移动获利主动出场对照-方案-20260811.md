# R-057 移动获利/主动出场 接入 vs 不接入 正式回测对照实验方案（2026-08-11）

> 老板要求：按回测标准做正式实验，量化"接入 vs 不接入"移动获利/主动出场的实际差距；**方案先交交易部独立审计确保无遗漏**；实验极其重要，必须考虑全部情况。

## 一、Part 1（前置门禁）：回测代码正确性验证——通过才可跑正式实验

### 1.1 重放框架 vs 引擎逐笔对账（核心）
- r57 重放框架（A 组全关模式：确认流程 delay2 + 出场=止损/hold 收盘）vs 引擎 `track_signal(phase_in=True)`——同信号集（r43_t2_T8）同窗口逐笔对比：
  - exit 价容差 ±0.01 / R 容差 ±0.005（对齐 r52 对账门禁）
  - **不一致即停**（禁止带病分析）
- 验证路径：确认 reject/stopped（两版同 R）、窗口边界（数据末占位触发）、停牌跳过计数

### 1.2 evaluate_exit 开关参数化默认零变化
- 4 开关 enable_breakeven/trailing/active/ttp，**默认全 True = 现状行为不变**（生产调用 sim_check/protect_card 不传参 = 现状；实验组显式传 False 关闭对应规则）
- 测试：默认参数输出与改造前逐字段一致

### 1.3 资金层模拟对账
- 重放资金模拟 vs r44.run_one 锚点：V0_26y = +1534.0%/-13.8%/946 笔、V0_7y = +801.1%/-16.3%/570、V0_3y = +302.2%/-12.0%/262——零差门禁
- 口径：8401×0.025×999、总资产曲线（build_total_asset_curve）、不定额注入关

### 1.4 全量测试回归（579 全绿）+ 验证报告落盘

## 二、Part 2：正式对照实验

### 1. 对比组（规则独立开关，分离边际贡献）
| 组 | 平保(1R) | 移动获利 | 主动出场 | TTP |
|---|---|---|---|---|
| **A 基线**（R-043 纯现状） | ❌ | ❌ | ❌ | ❌ |
| **B**（R-054 后现状） | ✅ | ❌ | ❌ | ❌ |
| **C** | ✅ | ✅ | ❌ | ❌ |
| **D** | ✅ | ❌ | ✅ | ❌ |
| **E 全开** | ✅ | ✅ | ✅ | ✅ |

### 2. 信号层矩阵（重放，r43_t2_T8 信号集）
- 5/10/20 窗 × {26 年全量 / 近 7 年(2019+) / 近 3 年(2023+)} × 5 组 = 45 格
- 指标：触发笔/胜率/avgR/累计R/累计R曲线回撤/误伤率（vs A）
- 对账门禁：A 组重放 vs CSV r_20d 逐笔一致（±0.005R）

### 3. 资金层矩阵（总资产口径回撤——回测规范 #2）
- A/B/C/E × {26 年 / 近 7 年 / 近 3 年} = 12 格，8401×0.025×999
- 重放出场序列 → 资金模拟（对齐 r44 口径）——指标：收益%/笔数/avgR/总资产回撤/selfcheck

### 4. 蒙卡（主对比组）
- A/B/C/E × 1 万次（同种子同 n，r_money 序列，对齐 R-052 版式）

### 5. 全覆盖项（审计重点关注清单）
- 0.5R 确认流程 delay2 所有组同源（reject/stopped 两版同 R——纯净对照）
- R-053 突破质量：本实验保持关闭（对照组纯净）
- 极值持久化：重放自维护 highest/lowest
- df 触发日切片（P0-3 契约）
- 成本：佣金万1.3 + 印花税万5（同基线）
- 数据边界：数据末占位触发标注
- 误伤口径：提前出场（基线未止损）且基线 R>0，含全部离场原因
- 停牌/数据缺失：跳过计数标注

## 三、产出
1. 验证报告（Part 1 门禁结果）——产出/输出/实验/r57/validation.md
2. R057 实验报告（45 格信号层 + 12 格资金层 + 4 组蒙卡 + 边际贡献分解 B−A/C−B/D−B/E−D）——R057-移动获利主动出场对照-20260811.md
3. 结论：每个规则接入/不接入的量化差距 + 建议

## 四、流程
方案落盘 → 交易部独立审计 → 意见吸收 → 老板批准 → Part 1 → Part 2 → 报告 → 呈报
