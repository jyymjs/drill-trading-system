# 策略版本存档（V1 现方案封存 · V2 C23 方案 · 2026-08-06 老板拍板）

> 状态：**V2（C23 方案）为现行方案**；V1（现方案/基线）**封存不删除**——资金量起来后
> 重新对比两套（2026-08-06 老板拍板）。
> 本文档是两套方案的参数唯一存档；恢复 V1 或对比时以此为准。

## 一、版本总览

| 版本 | 方案 | 状态 | 封存/启用原因 |
|---|---|---|---|
| V1 | S 级 + dn_confirm 1.5（无 C23） | **封存** | 资金约束下收益平庸（+12.2%）；资金量起来后重新对比两套时解封 |
| V2 | S 级 + dn_confirm 1.5 + **C23**（动量≤10% + 止损 0.5~3 元） | **现行** | 信号层 + 资金约束双口径全优（C23 替换进策略，2026-08-06 老板拍板） |

## 二、V1 现方案（基线）完整参数定义

- **评级**：prebreak 预突破 5 条件评级（PT/TY/DL/LK/SF），仅做 **S 级**（老板实盘约束）
- **dn_confirm（放量确认）**：1.5 —— 突破日量比 > 1.5 才计触发（量比 = 触发日成交量 ÷ 触发日前 20 日均量，不含触发日）
- **C23 收紧**：无（全信号进场）
- 其余口径：模式 prebreak / 观察窗 20d / 触发价 = TY 上沿 × 1.002 / 止损价 = TY 下沿 × 0.998 / 每股风险 = 触发价 − 止损价
- 扫描侧：prebreak 候选含放量阈值标注（前 20 日均量 × 1.5，手）；不设 C23 过滤
- 复现：`sim_capital.py --grades S --mode prebreak`（不带 --c23）

## 三、V2 C23 方案（现行）完整参数定义

- **评级**：同 V1（prebreak 5 条件，仅做 S 级）
- **dn_confirm**：1.5（量比 > 1.5 为起步线；**量比越高越好，1.5 仅作下限**——T-025 修正，
  全量口径下量比单调正贡献，不放上限）
- **C23 收紧**：
  - **动量 ≤ 10%**：触发价 vs 20 交易日前收盘涨幅 ≤ 10%（口径对齐 tighten_compare：
    `trigger / 触发日前第 20 根收盘 − 1`；扫描侧以最新日近似潜在突破日 → `trigger / iloc[-21] − 1`）
  - **止损距离 0.5 ~ 3 元**：`trigger − stop ∈ [0.5, 3.0]`（<0.5 太近易被扫，>3 太远盈亏比差）
- 扫描侧：prebreak 候选默认 **C23 过滤**（不达标不参与挂单候选，单独保存 `_c23` 后缀 CSV 供研究）；
  候选新增列 动量20日% / C23 / C23原因
- 复现：`sim_capital.py --grades S --mode prebreak --c23`

## 四、封存决策记录

- **2026-08-06 老板拍板**：C23（动量≤10% + 止损距离 0.5~3 元）替换进策略（S 级 + dn_confirm 1.5 + C23）；
  现方案（S 级 + dn_confirm 1.5，无 C23）**封存不删除**——资金量起来后重新对比两套。
- 数据依据（2026-08-06 T-024 + C23 资金约束对比，全市场 3 年，主口径 20d）：

| 口径 | 指标 | V1 现方案 | V2 C23 方案 | 差异 |
|---|---|---|---|---|
| 信号层（T-024，1441 笔触发基线） | avgR | 0.872 | **0.974** | +0.10 |
| 信号层 | 胜率 | 48.7% | 49.7% | +1.0pp |
| 信号层 | 信号留存 | 1441 笔（100%） | 519 笔（36.0%） | −64% |
| 资金约束（5600 元×1.5%×3 仓） | 总收益 | +12.2% | **+69.2%** | +57.0pp |
| 资金约束 | 成交 avgR | 0.124 | 0.683 | +0.559 |
| 资金约束 | 胜率 | 39.5% | 49.4% | +10.0pp |
| 资金约束 | 最大回撤 | 76.1% | 70.9% | −5.1pp |
| 资金约束 | 实际可执行 | 114 笔 | 87 笔 | −27 |
| 资金约束 | 年化笔数 | 35.8 | 27.3 | −8.4 |

- 结论草稿：收紧在资金约束下依然成立且更显著；代价是信号量衰减（36% 留存、100 笔节奏
  33.5→43.9 个月，资金层慢节奏范畴，仅作执行可行性参考）。**资金量起来后重新对比两套**——
  当前资金约束（5600 元）下 C23 挤压效应缓解（池右移、执行率 7.9%→16.8%），大资金下
  两套取舍可能变化，届时解封 V1 重跑对比。

## 五、实现与单一来源

- C23 条件常量单一来源：`项目/回测系统/tighten_compare.py`（DEFAULT_MOM=0.10 / RISK_MIN=0.5 /
  RISK_MAX=3.0，T-024 复算口径）；`c23_capital_compare.py`、`sim_capital.py` 直接引用；
  `分析决策/分析/scanner.py` 同值定义并注释引用（包路径限制，改动需两处同步）。
- mom20 复算：`tighten_compare.recompute/enrich`（duckdb 只读，引擎同口径）。
- 模拟：`sim_capital.py --c23`（信号集过滤，模拟核心逻辑零改动）。
- 扫描：`分析决策/分析/scanner.py`（C23 字段标注 + apply_c23_filter）、`main.py cmd_scan`（接入过滤）。
- 相关报告：`产出/输出/策略收紧测试-T024-20260806.md`、`产出/输出/C23资金约束对比-20260806.md`、
  `产出/输出/量比闸门交互矩阵-T025-20260806.md`、`产出/输出/dn_confirm_放量确认说明.md`。
