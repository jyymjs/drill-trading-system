# 策略版本存档（V2 现行唯一方案 · V1 已彻底清除 · 2026-08-07 老板拍板）

> 状态：**V2 为唯一现行方案**；V1（无 C23 基线）已**彻底清除**（2026-08-07 老板拍板：
> 不再封存、不再对比——旧方案参数定义删除，git 历史可追溯但不再作为可恢复方案）。
> 本文档是现行方案的参数唯一存档。

## 一、版本总览

| 版本 | 方案 | 状态 | 说明 |
|---|---|---|---|
| V1 | S 级 + dn_confirm 1.5（无 C23） | 🗑 **已清除**（2026-08-07） | 资金约束下收益平庸（+12.2%）；老板拍板彻底清除，不再对比 |
| V2 | S 级 + dn_confirm 1.5 + C23 + G1-G9 + delay2 + risk_mid 排序 | ✅ **现行（唯一）** | 全面测试 +107.3% 定稿 |

## 二、V2 现行方案（唯一）完整参数定义

- **评级**：prebreak 5 条件（PT/TY/DL/LK/SF），仅做 **S 级**（老板实盘约束）；Tier0 一票否决（结构<60根/完全释放>40%/通道/G1 跳空涨跌停/G2 一字形/无回踩）+ A1 蹭上去降级 + A2 数据不足观望
- **同日候选排序（2026-08-07 老板"好"确认替换，T-032 定案）**：当日多个可买入候选时按
  **每股风险居中**（|每股风险−1.5| 升序）优先——资金约束下可执行质量最优；
  +88.8% → **+107.3%**（真实回撤持平 31.2%、峰值更低 14.3%）；量比降序反降（熊市倒U实证）、
  S 数零差异；实现 = sim_capital `same_day_order="risk_mid"` + 执行卡候选队列排序
- **dn_confirm**：1.5（量比 > 1.5 为起步线；**量比越高越好，1.5 仅作下限**——T-025 修正，
  全量口径下量比单调正贡献，不放上限）
- **C23 收紧**：
  - **动量 ≤ 10%**：触发价 vs 20 交易日前收盘涨幅 ≤ 10%（口径对齐 tighten_compare：
    `trigger / 触发日前第 20 根收盘 − 1`；扫描侧以最新日近似潜在突破日 → `trigger / iloc[-21] − 1`）
  - **止损距离 0.5 ~ 3 元**：`trigger − stop ∈ [0.5, 3.0]`（<0.5 太近易被扫，>3 太远盈亏比差）
- **G1-G9 补齐**：G1 跳空/涨跌停排除、G2 一字形排除、G3 0.5R 分步建仓（环境弱日半仓起步→
  delay2 收线确认→补 0.5R）、G4 夹角、G5 TTP 前提、G6 主动离场环境、G7 三区间 5R、
  G8 像素感池级预筛、G9 资金 2.0%×3仓
- **确认规则**：delay2（首次收线不确认 → 允许第二根再确认一次；四处统一
  tracking/sim_trading/execution_card/sim_capital）
- **四道闸门**：B1 环境（指数-2%）/C3 量能（<5000万）/C4 情绪（>70%下跌）/C1 财报避让（默认全开）
- **资金配置（2026-08-08 老板确认更新）**：8401 资金（5401+3000 到账）+ 每月 3000 注入（本金积累）；**单笔风险 108 元（capital.json risk_ratio 1.29% 覆盖，不随资金机械上调）× 5 仓 × 整手**——完整周期回测 B2_5 定案（替代旧 2.0%×3仓）
- 其余口径：模式 prebreak / 观察窗 20d / 触发价 = TY 上沿 × 1.002 / 止损价 = TY 下沿 × 0.998 /
  每股风险 = 触发价 − 止损价
- 扫描侧：prebreak 候选默认 **C23 过滤**（不达标不参与挂单候选，单独保存 `_c23` 后缀 CSV 供研究）；
  候选新增列 动量20日% / C23 / C23原因
- 复现：`python 项目/回测系统/sim_capital.py --signals 产出/输出/sim_capital_20260806_full/signals.csv
  --grades S --mode prebreak --c23 --max-positions 3`（当前策略全链路见
  `产出/输出/当前策略全面测试-20260807.md`）

## 三、V1 清除记录（2026-08-07 老板拍板）

- 原"V1 封存待资金量起来对比"决策作废——老板拍板**彻底清除旧方案**：V1 参数定义删除，
  不再作为可恢复/可对比方案；git 历史（ac0ed5d 前）保留演进轨迹供追溯。
- 代码功能开关保留但标注实验用途：引擎 `--c23` 关闭 = 非现行方案配置（仅对照实验）；
  sim_capital `c23=False` 同理。**生产/回测/扫描一律走 V2 参数**（--c23 开启）。
- 原 V1 数据（+12.2% 等）仅存于 git 历史（2026-08-07 清理删除历史报告），不再引用。

## 四、实现与单一来源

- C23 条件常量单一来源：`项目/回测系统/tighten_compare.py`（DEFAULT_MOM=0.10 / RISK_MIN=0.5 /
  RISK_MAX=3.0，T-024 复算口径）；`c23_capital_compare.py`、`sim_capital.py` 直接引用；
  `分析决策/分析/scanner.py` 同值定义 + **运行时防漂移校验**（2026-08-07 接入，不一致即报错）。
- dn_confirm 甜点 1.5：引擎 `--dn-confirm` 参数 + 扫描标注（常量收敛留低优先待办）。
- mom20 复算：`tighten_compare.recompute/enrich`（duckdb 只读，引擎同口径）。
- 模拟：`sim_capital.py --c23 --half-phase --max-positions 3` + `same_day_order="risk_mid"`
  （生产链路，test_current_full.py 验证）。
- 扫描：`分析决策/分析/scanner.py`（C23 字段标注 + apply_c23_filter + 防漂移校验）、
  `main.py cmd_scan`（接入过滤 + full_card 落盘）。

## 五、后续修正（T-026 市场分段 / T-真实回撤重算 · 2026-08-06 质检建议级并入）

> 对既有表述的边界修正归档于此（V1 已清除，以下均为 V2 口径）：

**① 量比"越高越好"的边界——熊市例外（T-026 市场分段全景）**

- T-025 全量口径结论"量比越高越好（不放上限）"在分段下**牛市、震荡段成立**（牛段
  1.5~2.0 → 2.0~3.0 → >3.0 avgR 0.484 → 0.712 → 0.866；震荡段 0.822 → 1.700 → 2.963，单调递增）。
- **熊市倒 U**：量比 >3.0 组 avgR **0.513**，低于 2.0~3.0 组的 **0.774**——巨量突破在熊市是
  情绪化追高，"量比越高越好"在熊市**不成立**。
- 实操含义：dn_confirm=1.5 起步线维持不变；熊市若再收紧，量比上限方向优先于下限（当前
  扫描不做量比上限过滤，维持现状）。

**② 回撤口径修正——总资产口径（真实回撤重算）**

- **2026-08-06 铁律：现金口径已废弃，禁止再提**——回撤一律总资产口径（真实回撤 + 峰值回撤）。
- 当前策略真实回撤（对照链路重算）：**31.2%（占初始）/ 15.6%（占峰值）**，谷底 2026-07-13；
  回撤金额 1,748 元（资金层蒙卡最差情景一致）。
- 跨报告比较必须同口径。详见 `产出/输出/真实回撤-strict-vs-delay2-20260806.md` 等。
