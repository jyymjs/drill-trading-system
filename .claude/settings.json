{
  "customCommands": [
    {
      "name": "分身",
      "description": "启动分身并行模式 - 将大任务拆分为多个并行子任务加速完成",
      "prompt": "用户请求进入分身并行模式。请将以下用户的请求拆解为可并行的独立子任务，然后使用 Agent 工具同时启动多个子 agent 来并行处理。完成后汇总所有结果呈现给用户。\n\n用户请求：{{input}}"
    }
  ]
}
